```
python -m venv .env
source .env/bin/activate
pip install -r requirements.txt # installs fastapi for status page endpoint
python server.py # for server
python client.py # for client(s)
# access http://localhost:8000 for status page
```
